{
  "name": "mcp-micromind",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server for the MicroMind AI platform: folders, flows, prediction keys, capped predictions, extended reads. Full user power with tiered confirmations.",
  "type": "module",
  "bin": {
    "mcp-micromind": "./src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/run.mjs && node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.1"
  }
}
